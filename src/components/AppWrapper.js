import React from 'react';
import Feed from './Feed';

// This is the layer at which context and providers would probably be set.
const AppWrapper = () => (
    <Feed />
);

export default AppWrapper;
